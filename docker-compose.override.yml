# Coolify: use config.coolify.json, API key from PICOCLAW_PROVIDERS_OPENROUTER_API_KEY env
services:
  picoclaw-gateway:
    volumes:
      - ./config/config.coolify.json:/home/picoclaw/.picoclaw/config.json:ro
      - picoclaw-workspace:/home/picoclaw/.picoclaw/workspace
